#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main(){
	fstream inFile;
	string filePath;
	int c, l, w, i;

	cout << "Where is the file located? ";
	cin >> filePath;
	cin.get();

	inFile.open(filePath, ios::in);

	if (!inFile){
		cout << "Error! Please enter ensure you enter the proper file path." << endl << "Press enter to close the window...";
		cin.get();
		return 0;
	}
	
	else{
		protocol;
	}
	return 0;
}

void protocol(){

}
